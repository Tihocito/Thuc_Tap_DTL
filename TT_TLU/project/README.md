# CV & JD Extraction (Modular Version)

This project splits the old monolithic `ocr_cv.py` and `jd_extract.py` into small clean modules.

## Features
- Extract CV data via OCR (PaddleOCR)
- NLP rule-based extraction (skills, education, phone, email…)
- Export results to JSON + CSV
- Extract JD via interactive form
- Validate JD
- Export multiple JDs to a CSV

## Install

